# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# Get this from MongoDB Atlas:
# 1. Go to https://cloud.mongodb.com
# 2. Click "Connect" on your cluster
# 3. Choose "Connect your application"
# 4. Copy the connection string
# 5. Replace <password> with your database password
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/beautynomy?retryWrites=true&w=majority

# Example for local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/beautynomy

# Cuelinks Configuration
# Sign up at https://www.cuelinks.com to get your API credentials
# 1. Create a Cuelinks account
# 2. Go to Settings > API Integration
# 3. Copy your API Key (32 characters)
# 4. Copy your Publisher ID
CUELINKS_API_KEY=your_cuelinks_api_key_here_32_characters
CUELINKS_PUBLISHER_ID=your_publisher_id_here

# Admin API Key for Protected Endpoints
# Generate a secure random key (32+ characters recommended)
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# This key protects admin endpoints like scraping, price updates, etc.
ADMIN_API_KEY=your_secure_admin_api_key_here_32_characters_minimum

# ==================== ADVANCED SCRAPING CONFIGURATION ====================

# ScraperAPI (Optional - Paid Service)
# Bypasses anti-bot protection, handles CAPTCHAs, provides residential IPs
# Sign up at https://www.scraperapi.com for API key
# Pricing: ~$49/month for 100k requests (recommended for production)
# If not set, will fallback to Puppeteer or basic scraping
SCRAPER_API_KEY=your_scraperapi_key_here_optional

# Proxy List (Optional)
# Comma-separated list of HTTP proxies (format: host:port)
# Example: PROXY_LIST=proxy1.example.com:8080,proxy2.example.com:8080
# Free proxies: https://free-proxy-list.net (unreliable)
# Paid proxies: Bright Data, Smartproxy, Oxylabs (recommended)
PROXY_LIST=

# Puppeteer Configuration
# Set to 'true' to skip Puppeteer/Chrome installation (for platforms without Chrome)
# Note: This will disable JavaScript-heavy site scraping (Nykaa, Flipkart, etc.)
PUPPETEER_SKIP_DOWNLOAD=false
